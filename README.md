# BIGDATA_UABC
Git Flow Basics

## Basics Commands
```sh
git status
git add <.| -a>
git commit -a "A descriptive message"
git push
$ git clone <repo_name>

```
## Branch Commands
```sh
git checkout -b <branch_name>
git add <.| -a>
git commit -a "A descriptive message"
git push

```